const { resolve } = require('path'); // necessário utilizar o antigo método de import/export

module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'), // conexão com db
    'models-path': resolve(__dirname, 'src', 'app', 'models'), // as models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // as migrations
    'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'), // as seeds
}
